#ifndef CONSTANTS_H
#define CONSTANTS_H

// Mathematical constants
const float PI = 3.14159265359f;

// Body dimensions
const float TORSO_HEIGHT = 1.5f;
const float TORSO_WIDTH = 0.6f;
const float TORSO_DEPTH = 0.4f;
const float LEG_LENGTH = 0.8f;
const float LEG_RADIUS = 0.1f;

// Window dimensions
const int DEFAULT_WINDOW_WIDTH = 1024;
const int DEFAULT_WINDOW_HEIGHT = 768;

// Animation defaults
const float DEFAULT_DT = 0.01f;
const float DEFAULT_WALK_SPEED = 0.3f;
const float DEFAULT_ANIMATION_SPEED = 0.5f;

// Camera defaults
const float DEFAULT_CAMERA_DISTANCE = 8.0f;
const float DEFAULT_CAMERA_ANGLE_X = 20.0f;
const float DEFAULT_CAMERA_ANGLE_Y = 0.0f;

#endif // CONSTANTS_H